% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/if_ratio_quantiles.R
\name{if_ratio_quantiles}
\alias{if_ratio_quantiles}
\title{Influence Function for the Ratio Between Quantiles}
\usage{
if_ratio_quantiles(
  y,
  weights = NULL,
  type = 6,
  prob_numerator = 0.9,
  prob_denominator = 0.1,
  na.rm = TRUE
)
}
\arguments{
\item{y}{A numeric vector of data values.}

\item{weights}{A numeric vector of sampling weights (optional). If \code{NULL},
equal weights are assumed.}

\item{type}{Quantile estimation type: integer 4--9 or \code{"HD"} for
Harrell--Davis (default: \code{6}). See \code{\link{csquantile}}.}

\item{prob_numerator}{Numeric in \eqn{(0,1)}; order of the quantile at the
numerator (default: \code{0.90}).}

\item{prob_denominator}{Numeric in \eqn{(0,1)}; order of the quantile at the
denominator (default: \code{0.10}).}

\item{na.rm}{Logical; remove missing values before computing? Default: \code{TRUE}.}
}
\value{
A numeric vector of influence function values, one per observation.
}
\description{
Computes the influence function of the ratio between two quantiles
(e.g., P90/P10) for all observations in the sample.
See \insertCite{deville1999variance;textual}{inequantiles} and
\insertCite{osier2009variance;textual}{inequantiles} for the definition of
influence functions in finite population theory.
}
\details{
The influence function for the ratio \eqn{\widehat{R} = \widehat{Q}(p_n) / \widehat{Q}(p_d)}
is derived via the delta method applied to the quantile influence function of
\insertCite{deville1999variance;textual}{inequantiles}:

\deqn{
  \widehat{z}_i =
  \frac{
    \left(
      \frac{p_n - \mathbf{1}(y_i \leq \widehat{Q}(p_n))}
      {\widehat{f}(\widehat{Q}(p_n)) \, \widehat{N}}
    \right)
    \widehat{Q}(p_d)
    -
    \left(
      \frac{p_d - \mathbf{1}(y_i \leq \widehat{Q}(p_d))}
      {\widehat{f}(\widehat{Q}(p_d)) \, \widehat{N}}
    \right)
    \widehat{Q}(p_n)
  }{
    \widehat{Q}(p_d)^2
  }
}

where:
\itemize{
\item \eqn{\widehat{Q}(p)} is the weighted sample quantile of order \eqn{p},
computed via \code{\link{csquantile}},
\item \eqn{p_n} and \eqn{p_d} are the orders of the numerator and denominator
quantiles, respectively,
\item \eqn{\widehat{f}(\cdot)} is the estimated density function,
\item \eqn{\widehat{N} = \sum_i w_i} is the estimated population size.
}

The density \eqn{\widehat{f}(y)} is estimated via a Gaussian kernel:
\deqn{
  \widehat{f}(y) =
  \frac{1}{\widehat{N}\, h \sqrt{2\pi}}
  \sum_{j \in s} w_j
  \exp\!\left\{ -\frac{(y - y_j)^2}{2h^2} \right\}
}
with bandwidth \eqn{h = 0.79 \cdot \mathrm{IQR} \cdot \widehat{N}^{-1/5}}.
}
\examples{
# On synthetic data
set.seed(1)
eq_synth <- rlnorm(30, 9, 0.7)
IF_synth <- if_ratio_quantiles(y = eq_synth, prob_numerator = 0.80,
                               prob_denominator = 0.20)

# On survey data
data(synthouse)
eq <- synthouse$eq_income[1:30]
w  <- synthouse$weight[1:30]
IF_vals <- if_ratio_quantiles(y = eq, weights = w, type = 6)

}
\references{
\insertRef{deville1999variance}{inequantiles}
\insertRef{osier2009variance}{inequantiles}
}
\seealso{
\code{\link{ratio_quantiles}}, \code{\link{if_quantile}},
\code{\link{csquantile}}

Other influence functions: 
\code{\link{if_gini}()},
\code{\link{if_qri}()},
\code{\link{if_quantile}()},
\code{\link{if_share_ratio}()}
}
\concept{influence functions}
